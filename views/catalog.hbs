<div class="container">
  <div class="row">
    <div class="col-sm">
      {{#each data}}
        <div class="card" style="width: 18rem;">
        <img
          src={{{img}}}
          class="card-img-top" alt="...">
        <div class="card-body">
          <h5 class="card-title">{{name}}</h5>
          <p class="card-text">{{description}}</p>
          <a href=/products/{{_id}}/details class="btn btn-primary">Details</a>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</div>